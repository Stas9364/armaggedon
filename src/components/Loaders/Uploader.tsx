import React from 'react';
import style from './Uploader.module.css'

export const Uploader = () => {
    return (
        <div className={style.wrapper}>
            <span className={style.uploader}>Loading</span>
        </div>
    );
};

